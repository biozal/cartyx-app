  <nav class="topbar">
    <a href="/campaigns" class="topbar-brand">CARTYX</a>
    <div class="topbar-right">
      <% if (user.avatar) { %>
        <img src="<%= user.avatar %>" class="topbar-avatar" alt="">
      <% } else { %>
        <div class="topbar-avatar-placeholder">🧙</div>
      <% } %>
      <div class="user-menu">
        <button type="button" class="user-menu-trigger" onclick="toggleMenu(this)" aria-expanded="false" aria-controls="user-dropdown">
          <span class="topbar-name"><%= user.name || '' %></span>
          <span class="user-menu-arrow">▼</span>
        </button>
        <div class="user-menu-dropdown" id="user-dropdown">
          <a href="/settings" class="user-menu-item">⚙️ Settings</a>
          <a href="/logout" class="user-menu-item danger">🚪 Sign Out</a>
        </div>
      </div>
    </div>
  </nav>
