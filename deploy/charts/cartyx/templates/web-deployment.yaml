apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "cartyx.fullname" . }}-web
  labels:
    {{- include "cartyx.labels" . | nindent 4 }}
    app.kubernetes.io/component: web
spec:
  replicas: {{ .Values.web.replicaCount }}
  selector:
    matchLabels:
      {{- include "cartyx.web.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/secret: {{ include (print $.Template.BasePath "/secret.yaml") . | sha256sum }}
      labels:
        {{- include "cartyx.web.selectorLabels" . | nindent 8 }}
        {{- include "cartyx.data.podLabels" . | nindent 8 }}
    spec:
      securityContext:
        runAsNonRoot: true
        # Dockerfile.web runs as the `node` user (uid 1000).
        runAsUser: 1000
        # The data credentials mount 0440 and are owned by root, so the pod's user
        # reaches them through this group. Without it every read is EACCES.
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: web
          image: "{{ .Values.web.image.repository }}:{{ required "web.image.tag is required — set an immutable git-sha tag at install time" .Values.web.image.tag }}"
          imagePullPolicy: {{ .Values.web.image.pullPolicy }}
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          ports:
            - name: http
              containerPort: {{ .Values.web.env.PORT | int }}
          env:
            {{- range $key, $value := .Values.web.env }}
            {{- if $value }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
            {{- end }}
            # Server-side broadcasts (map:active-changed) reach the realtime
            # service by its in-cluster Service DNS name; the browser-facing
            # VITE_PUBLIC_PARTYKIT_HOST is baked into the client bundle and
            # is wrong inside the pod.
            - name: REALTIME_INTERNAL_HOST
              value: {{ printf "%s-realtime:%s" (include "cartyx.fullname" .) (.Values.realtime.env.PORT | toString) | quote }}
            - name: SESSION_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "cartyx.secretName" . }}
                  key: sessionSecret
            {{- include "cartyx.data.env" . | nindent 12 }}
            # The remaining secret keys are optional features (OAuth, R2).
            # `optional: true` lets an existingSecret omit them.
            - name: GOOGLE_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "cartyx.secretName" . }}
                  key: googleClientSecret
                  optional: true
            - name: GITHUB_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "cartyx.secretName" . }}
                  key: githubClientSecret
                  optional: true
            - name: R2_ACCESS_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: {{ include "cartyx.secretName" . }}
                  key: r2AccessKeyId
                  optional: true
            - name: R2_SECRET_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "cartyx.secretName" . }}
                  key: r2SecretAccessKey
                  optional: true
          livenessProbe:
            # /healthz does no I/O; a data-store outage must not restart the pod.
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            # /readyz probes the graph and Cassandra with a 2s server-side bound; timeoutSeconds
            # sits above it so a slow Atlas moment marks the pod unready
            # instead of racing the probe.
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            {{- toYaml .Values.web.resources | nindent 12 }}
          volumeMounts:
            {{- include "cartyx.data.volumeMount" . | nindent 12 }}
      volumes:
        {{- include "cartyx.data.volume" . | nindent 8 }}
