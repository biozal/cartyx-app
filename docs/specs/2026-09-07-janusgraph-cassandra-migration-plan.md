# MongoDB Atlas → JanusGraph and Cassandra migration

Date: 2026-09-07

Status: Local Docker and dev Kubernetes run the hardened database images. Production is active from immutable tag `data-v0.1.1`. Both environments passed independent off-host restores and reboot checks, and daily backups are enabled. No application subsystem has migrated. See [implementation evidence](2026-09-07-data-infrastructure-progress.md).

## Outcome and approach

Replace MongoDB Atlas with a self-hosted JanusGraph graph backed by Cassandra. Make Cartyx's campaign content addressable and connected: NPCs in a town, NPCs associated with a session, organization membership, quest participants, and wiki backlinks. Preserve the existing wiki and tabletop features while creating a foundation for richer relationships.

Start with infrastructure: Docker locally, then separate development and production deployments in Kubernetes through the existing GitOps workflow. Only after both environments pass persistence and recovery checks do we migrate application subsystems. Planned outages lasting several days are acceptable; preservation of existing data remains a requirement.

Use maintenance windows and a single authoritative backend per subsystem. Do not build a permanent Mongo/graph dual-write system. A phase is complete when its code, data, validation, and operational documentation are complete in dev and then prod. Keep later, dependent features unavailable where necessary until their phase is ready.

## What exists today

These findings come from the local application and sibling `cartyx-infrastructure` checkouts, not a live cluster or Atlas inventory. GitNexus has no index for this repository, so the inventory was traced directly from source.

| Area                     | Evidence and migration consequence                                                                                                                                                                                                                                                                                                                     |
| ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Deployments              | `docs/deployment.md` and the infrastructure README describe a single-node k3s cluster on `z440`, with namespaces `dev` and `prod`. Both environments share the physical host.                                                                                                                                                                          |
| Delivery                 | `.github/workflows/deploy.yml` builds images on `dev`/`main`, then updates image tags in `cartyx-infrastructure/apps/<env>/helmrelease.yaml`. Flux uses the corresponding application branch and `reconcileStrategy: Revision`. Keep application promotion unchanged; database releases are promoted independently from the infrastructure repository. |
| Local development        | `deploy/local/compose.yaml` currently starts web and realtime; `scripts/dev-all.mjs` starts host web/realtime processes. The audio worker has a separate launcher. Add database services to both local workflows, and add the worker to the full Compose stack.                                                                                        |
| Application persistence  | 33 Mongoose model files under `app/server/db/models`, with direct database operations throughout `app/server/functions`. Connection/bootstrap/index governance live under `app/server/db`.                                                                                                                                                             |
| Realtime persistence     | `realtime/src/history.ts` already exposes `HistoryStore`; `MongoHistoryStore` writes a separate `realtime_room_messages` collection. It is outside the Mongoose model inventory.                                                                                                                                                                       |
| Audio persistence        | `audio-worker/src/index.ts` uses Mongo directly. Claims, retries, fencing, abandoned uploads, and terminal writes depend on conditional database mutations in `claim.ts` and `process.ts`.                                                                                                                                                             |
| Relationships            | `Character` has a free-text `location`, legacy `sessionId`, a `sessions` array, and relationships with their own visibility. Locations maintain both parent and child arrays. Lore and quests have typed links; organization memberships are separate records.                                                                                         |
| Consistency              | Campaign, session, GM-screen, and tabletop functions use Mongo transactions. Schemas also enforce unique and partial indexes, including one in-progress session per campaign. These need explicit replacements.                                                                                                                                        |
| Search                   | Many wiki functions use `$text`; audio also uses regex search and aggregation for quotas. Search/filter/sort behavior must be inventoried, not translated mechanically.                                                                                                                                                                                |
| Authentication and media | Login sessions are signed JWT cookies (`app/server/session.ts`); user identity and membership are persisted. R2 stores media. Preserve IDs, session signing configuration, encrypted token handling, media keys, and per-owner storage prefixes.                                                                                                       |
| Tooling                  | Mongo-specific seeds, fixtures, cleanup scripts, imports, health checks, dependency guards, and all three package manifests are in scope.                                                                                                                                                                                                              |

The infrastructure checkout still contains worker image placeholders; validate actual deployed revisions during the first phase rather than treating checkout values as live state. The infrastructure README also states that `local-path` PVC sizes are not disk quotas.

## Target architecture

```text
Browser → existing web / realtime endpoints
                   │
          application repositories
             ┌─────┴──────────────┐
             │                    │
       Gremlin client         CQL client
             │                    │
      JanusGraph Server           │
         │          │             │
         │     search index       │
         │   (Elasticsearch)      │
         │                        │
         └──────── Cassandra ─────┘
                    │
         graph keyspace + separate
         application-state keyspace

R2: existing media + private off-host database backups
Flux: separate dev and prod releases in the existing cluster
```

**Storage ownership:** JanusGraph owns entity content and relationships. Cassandra is its storage backend through `storage.backend=cql`; application code must never modify JanusGraph's internal tables. Dedicated application-owned CQL tables hold operational records whose access and concurrency requirements suit them: ordered room history, job leases, and selected conditional state. Give those tables a separate keyspace, schema migrations, and credentials. JanusGraph documents this remote server/CQL architecture in its [Cassandra backend guide](https://docs.janusgraph.org/storage-backend/cassandra/).

Schema migration journals use a separate admin-only `cartyx[_dev|_prod]_schema` keyspace because the runtime state role inherits MODIFY on tables in its state keyspace. This metadata is included in full-volume backups. See the [Cassandra state foundation](2026-09-13-cassandra-state-foundation.md) for the version/owner protocol.

Every persisted field gets one authority. For example, an audio job row owns processing status and lease state, while an audio vertex owns wiki metadata and relationships. Graph summaries of operational state are derived projections, updated by an idempotent, recoverable process. R2 object deletion consults authoritative ownership and references, never a potentially stale projection alone.

**Search:** bring up JanusGraph and Cassandra first. Add a supported Elasticsearch mixed-index backend before migrating the first subsystem with full-text search. Exact identity lookup and relationship traversal can initially use composite and vertex-centric indexes. JanusGraph distinguishes these from mixed indexes needed for indexed text and range queries; Cassandra alone does not preserve Mongo `$text` behavior. [Index guidance](https://docs.janusgraph.org/v1.1/schema/index-management/index-performance/), [text-search semantics](https://docs.janusgraph.org/index-backend/text-search/).

**Versions:** Phase 0 must produce an exact tested matrix and pinned image digests for JanusGraph, Cassandra, JDK/container architecture, Gremlin JavaScript client, serializer, and Elasticsearch. The published JanusGraph 1.1 compatibility matrix lists Cassandra 3.11/4.0 and TinkerPop 3.7; this is a research baseline, not a recommendation to deploy an old bundled patch. Resolve compatibility against currently maintained/security-patched releases and test the selected combination; do not assume Cassandra 5, TinkerPop 4, or an OpenSearch substitution works. [JanusGraph compatibility and releases](https://docs.janusgraph.org/changelog/).

### Local and Kubernetes layout

| Environment     | Initial topology                                                                                                      | Persistence and isolation                                                                                                                                                              |
| --------------- | --------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Local Docker    | One Cassandra, one JanusGraph; Elasticsearch when search is introduced; web/realtime/audio worker in the full stack   | Named volumes; dedicated local keyspaces and credentials. Bind optional database host ports to loopback. Container clients use service DNS; host dev processes use loopback endpoints. |
| Kubernetes dev  | One Cassandra StatefulSet replica and one JanusGraph Deployment replica, namespace `dev`                              | Dedicated PVCs, secrets, graph/state keyspaces, backup prefix, and search instance.                                                                                                    |
| Kubernetes prod | Separate one-replica Cassandra StatefulSet and JanusGraph Deployment, namespace `prod`, initially on current hardware | Dedicated PVCs, secrets, graph/state keyspaces, backup prefix, and search instance. Independently restorable from dev.                                                                 |

Use replication factor 1 while there is only one Cassandra node per environment; select explicit datacenter/replication and consistency settings that work with that topology. Three pods on `z440` would not survive loss of `z440`. Future HA is a separate milestone: at least three independent hosts/storage failure domains, RF 3, quorum settings, tested repair, and multiple JanusGraph instances. Change existing keyspace replication deliberately; changing a startup default does not migrate it.

The separate `cartyx-infrastructure` repository owns `deploy/charts/cartyx-data`, its local/dev/prod values, Docker definitions, operations tools, tests, and infrastructure CI. Its `data/dev` and `data/prod` HelmReleases are reconciled independently of the application releases. Dev reads the infrastructure main branch through `flux-system`; prod reads a specifically promoted `data-v*` tag through `cartyx-data-prod`. Publish a new tag only from an infrastructure revision validated in dev, and update the production source to that tag. Never move an existing release tag. The app repository retains local startup wrappers, the Mongo inventory, and application migration code. Pin the tested published database image digests. The infrastructure image-build workflow maintains the minimal runtime and dependency security patches, with native architecture scans and recovery tests. Do not repurpose the three existing application CI image-tag markers.

Declare data → schema job → dependent app rollout ordering without a circular dependency. Separate Flux Kustomizations where necessary so failure of a data rollout does not prevent unrelated resources from reconciling. Keep the existing app independent of the new database until a subsystem actually uses it.

Implementation evidence and remaining gates are tracked in
[the rollout record](2026-09-07-data-infrastructure-progress.md). Checklists below
retain the full acceptance scope; a partially completed phase is not an exit.

## Phase 0 — Inventory, compatibility, and capacity

- [x] Record actual node CPU/RAM, free disk/inodes, filesystem/device layout, StorageClass reclaim behavior, architecture, Kubernetes versions, and current pod resource consumption. The app chart's documented 62 GiB host is a starting clue, not a live measurement.
- [ ] Inventory each Atlas environment read-only: collections, document counts, byte sizes, all indexes/TTL/partial constraints, orphan references, free-text location values, and existing backup options. Include raw-driver collections and encrypted/hidden fields.
- [ ] Produce a field/query ownership matrix: graph properties/edges, CQL state, retained media, derived values, and replacements for every Mongo transaction/conditional update.
- [ ] Run a disposable compatibility spike: JavaScript Gremlin create/read/update/delete, edge IDs and serialization, reconnect, authentication, schema installation, and persistence through server restart. Verify both developer-machine and cluster image architectures. Use the official [container configuration guidance](https://github.com/JanusGraph/janusgraph-docker/blob/master/README.md).
- [ ] Set measured resource requests/limits and JVM heap below container memory limits with native-memory/page-cache headroom. Size disks for data, indexes, commit logs, compaction, snapshots, and backup staging for both environments together.
- [ ] Record backup targets. Implemented initial policy: nightly off-host backups, an additional backup before every cutover/upgrade, every completed backup for 14 days and one per UTC week through day 56; steady-state RPO ≤24 hours and target RTO ≤4 hours, verified by restore timing. Accepted migration downtime does not silently set the long-term recovery policy.

**Exit:** tested versions, collection/constraint inventory, resource budget, disk budget, and recovery runbook outline are committed. Resolve version/support gaps before committing to a production image set.

## Phase 1 — Local database infrastructure

- [x] Add Cassandra and JanusGraph to `deploy/local/compose.yaml`, with named volumes, explicit backend/keyspace configuration, non-default credentials, and health checks that perform CQL/Gremlin operations.
- [x] Use long startup allowances for Cassandra/JVM boot. JanusGraph waits for Cassandra readiness. Liveness checks detect a hung process; dependency outages should affect readiness without a restart cascade.
- [ ] Add an idempotent one-shot bootstrap/smoke service: create a small test schema, two vertices and an edge, read the relationship, and clean up its fixture.
- [x] Add explicit `db:up`, `db:down`, `db:status`, and `db:smoke` commands. Make `npm run dev` start/wait for the Docker database dependencies before launching host processes. Ctrl+C stops the host processes; database volumes survive. Keep destructive reset a separate explicit command.
- [x] Add the audio worker to the full Compose path and document its media credentials. Keep host-process and full-container modes clear to avoid competing workers or port 1999 collisions.
- [x] Update `.env.example`, `deploy/local/README.md`, and the kind deployment path. Supply server-only Gremlin/CQL settings; no database secrets or endpoints in `VITE_PUBLIC_*`.

**Exit:** a fresh checkout can start the dependencies, run the smoke query, restart containers, and read the same persisted relationship. Normal teardown/recreation preserves data. Existing Mongo-backed app behavior still works in this infrastructure-only phase.

## Phase 2 — Kubernetes development infrastructure

- [x] Implement the data chart: Cassandra StatefulSet, stable/headless discovery service, PVCs, JanusGraph Deployment/ClusterIP service, config maps, secret references, service accounts, probes, resources, and graceful shutdown.
- [x] Configure retained volumes and PV reclaim behavior explicitly. Review Helm uninstall, StatefulSet replacement, and Flux prune paths; prove that an ordinary chart upgrade cannot discard database data. Protect production data resources from accidental pruning.
- [x] Add namespace-scoped network policy allowing only intended application, migration, monitoring, and database traffic; allow required DNS. Verify enforcement in k3s. No public ingress, tunnel route, or NodePort for Gremlin/CQL/search/JMX. Test credentials and encrypted connections for cluster traffic.
- [ ] Keep schema administration credentials separate from runtime credentials. Bootstrap pre-created keyspaces/tables as needed, then reduce privileges. Store secrets out of Git using the existing named-secret convention; document rotation/restarts.
- [x] Add the dev data HelmRelease and reconciliation ordering in the infrastructure repository. Allow enough Helm/job time for initial database startup.
- [x] Extend Helm rendering/validation for local/dev/prod configurations, secret references, storage retention, required settings, and forbidden public services; add real container integration coverage in CI.
- [ ] Connect metrics/logs to the existing platform: query errors/latency, heap/GC, Cassandra timeouts, disk/inodes, compaction backlog, backup age, and restore outcomes. Label by environment without logging private content.

**Exit:** Flux reports the actual data release Ready, the in-cluster graph smoke test passes, and data survives JanusGraph/Cassandra pod replacement and a dev chart upgrade. Dev credentials cannot read prod. No application cutover yet.

## Phase 3 — Production infrastructure and recovery

- [x] Tag the infrastructure revision verified in dev, then promote that tag through `clusters/z440/data-source-prod.yaml`. Enable the production source and HelmRelease only after recovery gates pass. Provision separate prod secrets and storage.
- [x] Implement scheduled Cassandra backups for graph and operational keyspaces, schema, and required system/auth metadata. Upload manifests, checksums, version/config references, and data to private R2 prefixes per environment. Keep credentials in the secret recovery process.
- [x] Use a Cassandra-aware consistent backup and upload it off-host. The initial implementation drains and stops Cassandra after stopping graph writers, then archives the full data directory and recovery credentials. This deliberately accepts backup downtime. If moving to online snapshots later, account for snapshots being hard links on the source disk and validate write consistency and off-host recovery separately. [Cassandra backup guidance](https://cassandra.apache.org/doc/4.0/operating/backups.html).
- [x] Rehearse restoration into a new isolated instance/volume from only off-host backups and documented secrets. Restore graph and CQL state to the same checkpoint; rebuild search indexes when present. Verify records, relationships, permissions, and timed recovery.
- [x] Test orderly node reboot in dev first, then during a production maintenance window. Separate this persistence check from the empty-volume restore drill.
- [ ] Verify live deployment revisions, alert delivery, backup retention/cleanup, and disk budgets. Public HTTP 200 from the current deploy workflow is insufficient evidence of database readiness.

**Exit:** both Kubernetes environments are operational, independently persistent and restorable, with a recorded successful off-host restore. This is the first major deliverable and the stopping point before application replacement work begins.

## Phase 4 — Graph foundation, search, and migration tooling

### Schema and relationship rules

Keep current API identifiers as strings: preserve each Mongo `_id` value as an application `entityId`; let JanusGraph manage its internal vertex IDs. Define a unique indexed identity tuple including entity kind and scope, with an explicit scope for global/user-owned records. Do not expose Gremlin IDs in routes or references. Retain existing ID format for newly created records while legacy ObjectId consumers remain, then change validators in a separate migration if desired.

Use explicit labels/property types/cardinality, disable accidental schema creation, and run versioned schema changes through a single migration job. Wait for index availability, reindex existing data when required, and fail production queries that require an unplanned full graph scan. Graph properties should hold queryable values and content; bounded nested UI values may use versioned JSON. Do not hide traversable relationships inside JSON.

| Meaning                                  | Proposed canonical relationship                                                                                                                  |
| ---------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| Content belongs to a campaign            | `Entity → IN_CAMPAIGN → Campaign` plus indexed campaign scope                                                                                    |
| Building/neighborhood is inside a town   | `Location → WITHIN → Location`; incoming traversal replaces the duplicated child array                                                           |
| NPC's current location                   | `Character → LOCATED_IN → Location`                                                                                                              |
| NPC associated with a session            | `Character → ASSOCIATED_WITH → Session`, with source/reason/visibility                                                                           |
| Player or NPC belongs to an organization | Membership relation with title/public/private notes; preserve the membership's existing ID using a relation vertex where independently addressed |
| Quest giver/participant or hierarchy     | `GIVEN_BY`, `INVOLVES`, `SUBQUEST_OF`                                                                                                            |
| Wiki reference/backlink                  | `LINKS_TO` with relationship metadata; backlinks use incoming traversal                                                                          |
| Tagging                                  | `TAGGED_WITH` campaign-scoped normalized tag vertices                                                                                            |
| Map token represents an entity           | `REPRESENTS`; token placement remains an instance, not the identity of the NPC                                                                   |

Make all meaningful records linkable, but do not create all possible pairs. Define allowed endpoints, multiplicity, direction, visibility, deletion behavior, and provenance for each relationship type. Preserve ordering where UI arrays are ordered. Reify a relationship as a vertex when it needs its own ID, history, or multiple pieces of evidence.

`LOCATED_IN` means current placement. Residence and historical session placement need distinct semantics (`RESIDES_IN` or dated/session-linked occurrences); do not overwrite history when an NPC moves. A town query includes the town and nested locations with cycle protection, pagination, and authorization at every step. Session associations include explicit legacy references and supported event/document references; record why the NPC is associated rather than treating every mention as physical attendance.

Match free-text NPC locations only to an unambiguous same-campaign location. Preserve original text; produce unresolved/ambiguous mappings for manual correction. Reconcile contradictory parent/child arrays and deduplicate `sessionId`/`sessions` references without silently discarding provenance. Dangling references become reported migration exceptions, not fabricated entities.

Enforce campaign membership, entity visibility, relationship visibility, GM-only fields, and player edit permissions in server repositories. A visible NPC does not make a private relationship visible. Global SRD records and user-owned audio need explicit scope rules. Do not expose a general Gremlin endpoint to browsers. Search, totals, backlinks, and multi-hop paths must not reveal hidden entities or relationships.

### Application boundary and correctness

- [ ] Add server-only domain repositories under proposed `app/server/repositories/`, plus graph/CQL clients and codecs under `app/server/db/`. Preserve existing server function request/response DTOs. No Mongoose types in the new domain interfaces.
- [ ] Extract a Mongo adapter only when needed for the next slice; do not rewrite every model abstraction up front. Route each subsystem explicitly to Mongo, graph/CQL, or maintenance. No silent fallback to stale Mongo data.
- [ ] Install the search service in Compose and both data releases with the same persistence/security practices. Implement text/filter/sort parity tests before the first wiki slice; search ranking may differ, but no feature silently disappears.
- [ ] Define bounded traversals, stable pagination, batched fetching, timeout/cancellation/reconnect handling, and idempotency for retries with uncertain commit outcomes. Use client traversals/bound parameters, never interpolated user scripts. Separate schema/admin execution from runtime traversal access.
      The [Gremlin authorization handler prerequisite](2026-09-14-gremlin-authorization-prerequisite.md) tracks the request-isolation backport in infrastructure PR #14; candidate promotion and the server-enforced runtime policy remain required.
- [ ] Replace Mongoose defaults, validation, hooks, sparse/partial uniqueness, timestamps, projections, and error mapping explicitly. Inventory schemas directly: the existing index governance registry is not the complete collection list.
- [ ] Design uniqueness and conditional-state operations before moving auth or workers. JanusGraph on Cassandra does not provide Mongo-equivalent transaction isolation; graph locking requires deliberate configuration and has failure limits. [JanusGraph consistency guidance](https://docs.janusgraph.org/advanced-topics/eventual-consistency/).
- [ ] Use application-owned Cassandra lightweight transactions for selected compare-and-set invariants: key reservations, revisioned control state, and job fencing. Define partition keys, contention limits, serial/read/write consistency, retry behavior, and reservation recovery. These are not arbitrary cross-partition transactions. [Cassandra guarantees](https://cassandra.apache.org/doc/stable/cassandra/architecture/guarantees.html).
- [ ] For multi-record operations, specify one authoritative control record plus recoverable/idempotent steps. Examples: campaign creation, membership changes, one active session, screen reorder, and graph/job projections. Return success only at the documented committed state; repair incomplete work after crashes. Never infer authorization from a stale graph projection when authoritative membership has been revoked.

**Exit:** schema/clients/migrator work against actual containers and dev Kubernetes; search is deployed; repository contract, isolation, duplicate-write, and failure-recovery tests pass. Production code remains on its previous subsystem authorities until each cutover below.

## Application migration sequence

Execute each row as its own slice within the single ongoing application PR #557 against `dev`. Within each slice: implement and test → rehearse in dev → migrate dev → validate → promote code/schema → migrate prod → validate. Production code must remain in the explicit old/maintenance mode until data for that slice is ready. The [identity source preflight](2026-09-13-identity-source-preflight.md) adds snapshot BSON export, offline verification and the first live account/membership audit. The [identity repository extraction](2026-09-13-identity-repository.md) routes request-time identity consumers, login, token storage, preferences, media prefixes and transitional membership mirrors through Mongo-backed contracts. The [identity availability boundary](2026-09-14-identity-availability.md) separates identity readiness from remaining domain Mongo checks and guards each runtime repository operation. Target readiness/health and replacement of the existing Mongo transaction dependency remain ahead of target storage selection. The inactive [identity reservation journal](2026-09-13-identity-reservations.md) now provides identifier uniqueness and preparation recovery; the inactive [account state protocol](2026-09-13-identity-account-state.md) adds binding/token revision checks and durable receipts. The inactive [graph profile publication/read facet](2026-09-13-identity-graph-profiles.md) adds recoverable linked graph revisions and target lookups. The [identity import rehearsal](2026-09-13-identity-import-rehearsal.md) adds offline source projection checks and create-only, recoverable bound/unbound imports exercised with synthetic data. The [target settings writes](2026-09-13-identity-settings-writes.md) add revisioned preference updates and recoverable media namespace allocation. The [private bulk package and runner](2026-09-13-identity-bulk-import.md) now retain exact plans and the complete archive under an environment/endpoint/CA binding, with explicit batch recovery and read-only receipt/value inspection before operator recovery. Inspection reports archive ordinals and conservative statuses, never repairs or authenticates, and does not establish a cutover snapshot. Target login, token clearing, provider attempts and admission barriers have inactive coordination protocols. Runtime integration, safe provider resolution/reauthorization, final source import/reconciliation, runtime authorization and fenced release remain. These changes do not apply source accounts or cut over a subsystem.

| Order | Subsystem and main files                                                                                                           | Data/behavior and acceptance gate                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ----- | ---------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 5     | Identity/access: `User`, middleware, `requireCampaignMember.ts`, OAuth utilities, user/admin functions                             | Preserve accounts, provider identifiers, encrypted tokens, roles, preferences, media prefixes, and JWT ID compatibility. Keep campaign access behind an adapter to Mongo until phase 6. Verify concurrent first login, account lookup, hidden fields, and permission denial.                                                                                                                                                                                                                                                                           |
| 6     | Campaigns and membership: `Campaign`, `functions/campaigns.ts`, membership checks in player functions                              | Establish campaign roots and member relationships. Choose one authoritative membership representation; preserve invite uniqueness, role changes, player limits, and cascade behavior. Cross-store destructive operations stay in maintenance until all dependent cleanup adapters exist.                                                                                                                                                                                                                                                               |
| 7     | Locations and classification: `Location`, `LocationType`, `Tag`, `functions/locations.ts`                                          | Migrate hierarchy first, normalize tags, detect cycles/conflicting arrays, and preserve content/visibility/search. Lists, tree edits, deletion policy, and incoming child traversal pass.                                                                                                                                                                                                                                                                                                                                                              |
| 8     | NPCs and player characters: `Character`, `Player`, character/player functions                                                      | Migrate NPC relationships and location links, player ownership/edit rules, crop/status/nested fields. Introduce a location entity picker while preserving unresolved text. Town page can list all authorized NPCs in the town and descendants. References to sessions remain in the migration reference ledger until phase 9.                                                                                                                                                                                                                          |
| 9     | Sessions and notes: `Session`, `Note`, `SessionEvent`, session/note functions                                                      | Migrate scheduling/lifecycle and notes; replace session-number allocation and one-active-session constraint with tested conditional state. Convert session associations and typed event links; preserve event order and visibility. Session page lists all authorized associated NPCs with reasons, without duplicates.                                                                                                                                                                                                                                |
| 10    | Organizations: `Organization`, `OrganizationMembership`, organization functions                                                    | Migrate memberships, titles, private notes, and location relations. Preserve membership IDs and uniqueness; member edits and backlinks pass.                                                                                                                                                                                                                                                                                                                                                                                                           |
| 11    | Calendars/events: `Calendar`, `Event`, calendar/event functions                                                                    | Preserve custom calendar configuration, dates/order, parent/linked events and entity links. Calendar editing, timeline filtering, and deletion cleanup pass.                                                                                                                                                                                                                                                                                                                                                                                           |
| 12    | Lore: `Lore`, lore functions and `pruneLoreLinks.ts`                                                                               | Migrate content, typed references, images, permissions, and search. Resolve links to not-yet-migrated reference records through the ledger. Preserve inbound/outbound navigation without leaking GM content.                                                                                                                                                                                                                                                                                                                                           |
| 13    | Quests: `Quest`, quest functions                                                                                                   | Migrate givers, participants, hierarchy, linked events, progress and public/private text. Traverse quests by NPC, town, organization, or session where relationships exist.                                                                                                                                                                                                                                                                                                                                                                            |
| 14    | Rules/reference content: `Race`, `Monster`, `Spell`, `Rule`, SRD and seed/import functions                                         | Migrate global/campaign scope and customizations, preserve attribution/content and nested rules/stat blocks. Resolve reference links from earlier phases. Import is repeatable; browsing/search and tabletop consumers retain their DTOs.                                                                                                                                                                                                                                                                                                              |
| 15    | GM screens and tabletop: `GMScreen`, `TabletopScreen`, `TabletopPlayerState`, `Map`, `MapToken`, `MapText`, `MapDrawing`, `MapAoE` | Replace collection/document references with typed entity resolution. Preserve independent token instances, active map, tabs/order, fog/visibility and player state. Use revisioned CQL records where needed for mutable control state. Verify reload, concurrent edits, deletes, and multi-browser play.                                                                                                                                                                                                                                               |
| 16    | Realtime history and play logs: `realtime/src/history.ts`, `Message`, `DiceRoll`, relevant session functions                       | Implement `CassandraHistoryStore` using room-scoped bounded partitions and sequence clustering. Account for room sequence recovery, deduplication, retention/pruning, and GM channels. Distinguish chat/dice records from raw room envelopes so imports do not duplicate them. Keep realtime replica count at 1 until room routing/state sharing is separately redesigned.                                                                                                                                                                             |
| 17    | Audio and soundboard: `AudioAsset`, `AudioPackage`, `SoundboardState`, audio/package/soundboard functions and worker               | Graph owns assets/packages and links; dedicated CQL state owns authoritative job status/leases and revisioned playback state. Model queryable pending work in bounded queue partitions; use conditional claims with generation fencing on heartbeat/retry/reap/terminal transitions. Reconcile queue indexes/projections after failure. Preserve variants, backoff, retry caps, quotas, storage prefixes, cleanup and R2 references. Concurrent-worker, stale-worker, delete-during-process, crash/replay, and soundboard stale-write tests must pass. |
| 18    | Tooling and Mongo removal                                                                                                          | Finish every seed/fixture/import/admin/cleanup path, health check, package manifest, environment variable, and deployment document. Prove web, realtime and worker work with Mongo inaccessible before Atlas retirement.                                                                                                                                                                                                                                                                                                                               |

Cross-phase references use an explicit ledger keyed by environment, source kind/ID, target kind/ID, relationship type, and metadata. Import endpoints when their subsystem migrates, then resolve pending edges idempotently. Do not expose incomplete placeholder vertices to users. Temporarily retain original reference values and read-through repository resolution where an existing view needs them; otherwise return a clear maintenance state. After the final slice, no unreviewed pending references remain.

Seeds and fixtures move with their owning subsystem, not only at phase 18. Every slice includes inbound references, permission checks, cleanup paths, and callers that previously used `.populate()` or direct model access. A collection is not retired while another subsystem still writes it.

## Repeatable cutover and rollback procedure

1. **Prepare:** pin source/target application, schema, data chart, and image revisions; record a per-environment manifest of authoritative backends. Rehearse on synthetic or appropriately sanitized data and measure migration time. Never seed dev directly with production secrets or private content.
2. **Quiesce:** put the slice and dependent mutations into maintenance; use whole-app maintenance if dependencies cannot be isolated safely. Drain/stop affected workers, WebSocket writes, seed scripts, cleanup jobs, and old app replicas. Record zero remaining writers. Express sustained maintenance/replica changes in GitOps so Flux cannot undo them.
3. **Capture:** take a restorable Atlas export/backup suitable for the actual tier plus a target checkpoint. Export raw data with BSON/Extended JSON type fidelity, preserving IDs, timestamps, null-versus-missing, nested arrays, secrets, and all required fields. Hash/count each collection. No committed dumps or secrets.
4. **Transform:** load vertices first, then edges and CQL records. Use deterministic identities, checkpoints, bounded batches, retries, a reference ledger and quarantine reports. Re-running after interruption must not duplicate vertices, relationships, jobs, or history. Upsert alone does not handle source deletions: reconcile removals against the frozen source manifest and tombstone/delete stale target records deliberately.
5. **Validate:** compare all relevant property values using normalized hashes, entity counts by kind/campaign, edge counts by label, timestamps, media keys, and orphan reports. Map each source index/constraint to its replacement. Run permission, search, CRUD, ordering, relationship, concurrency, restart, and recovery checks. Resolve/quarantine every discrepancy explicitly; global counts alone are insufficient.
6. **Switch:** activate the slice's backend and deploy compatible web/worker/realtime consumers together as required. Smoke-test against actual target storage, then reopen traffic. Record completion in the migration manifest and take a post-cutover backup.
7. **Observe:** verify errors, search/index freshness, latency, queue progress, and data reconciliations against agreed limits. Use representative campaign-size load tests; set performance thresholds from measured existing behavior before each slice.

Before reopening writes, rollback can restore the target checkpoint and reactivate the frozen Mongo slice. After new writes are accepted, flipping a flag back to Mongo would lose data. Re-enter maintenance and either repair forward (default) or run a rehearsed reverse export/replay that preserves all accepted writes. Keep an application-owned operation journal/checkpoint for migrated mutations when reverse rollback is required; define how it remains recoverable if graph and journal updates fail between steps. Do not promise lossless reverse migration without demonstrating it.

Leave old Mongo collections frozen for the agreed retention period; do not delete collections during individual code PRs. Expanding an old subsystem back into service requires reference reconciliation with already migrated subsystems.

## Completion criteria

- Local Docker, optional kind, Kubernetes dev, and Kubernetes prod all run the same tested storage architecture and schema versions.
- Every existing persisted collection has a documented destination and verified import; every production read/write path uses the new storage. No runtime Mongo requirement remains in web, realtime, audio worker, fixtures, or operational tooling.
- Demonstrate: all visible NPCs in a town including nested buildings; all visible NPCs associated with a session; organization memberships; quest participants; and wiki backlinks. Moving an NPC does not rewrite historical session facts.
- Permission tests prove no cross-campaign access or disclosure through private edges, search snippets/totals, GM fields, or backlinks.
- Re-run imports and interrupted operations without duplicates; enforce identity/active-session/worker-fencing constraints under concurrency; pass multi-browser tabletop and audio scenarios.
- Recovery from an off-host backup onto empty storage succeeds within the recorded objective. Alerts identify failed/stale backups and low disk before an outage.
- After a proposed 14-day validation period with Mongo disconnected, take and verify final archival exports, remove Mongo secrets/dependencies/automation, and retire both Atlas environments. Atlas deletion is a distinct final operational step after the checklist is signed off.

## First implementation work package

Implement phases 0–3 before any domain persistence rewrite. Deliver the compatibility/capacity record, Docker services and commands, `cartyx-data` chart, dev/prod Flux releases, backup/restore automation, dashboards, runbooks, and persistence/recovery evidence. Do not bundle NPC schema conversion, Mongo data imports, or application backend switching into that infrastructure work package.

Track the later rows as individual implementation work items with the acceptance gates above. Schedule by passed gates rather than calendar promises; compatibility, existing data quality, and the operational-state rewrites will determine duration.
